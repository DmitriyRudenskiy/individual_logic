var loader = require('./util/loader');
var text = require('./util/text');
var html = require('./util/html');

// console.log(loader.ping());

var listWord = text.showText('Привет, как дела? Что кушал сегодня.');
console.log(listWord);